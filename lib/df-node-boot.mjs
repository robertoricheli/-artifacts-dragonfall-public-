/**
 * Carrega DfRules / DfData / DfEffects / DfEngine via motor TypeScript (Node).
 * No pacote Render: lib/ → motor/ (raiz do repo).
 */
export { bootDragonfallEngine, clearEngineCache } from "../motor/boot-engine.js";
